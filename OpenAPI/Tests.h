#pragma once
#include"Matrix.h"

void Get_matrix_form_file(Matrix& a, Matrix& l);

Matrix start_alg(Matrix& mat, int alg, double& time, size_t a = 128, int b = 64, int c = 32);

void start_home(const bool, size_t);

